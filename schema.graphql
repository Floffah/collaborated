# This file was generated based on ".graphqlconfig". Do not edit manually.

schema {
    query: RootQueryType
}

type AuthDetails {
    "The main access code for the current session"
    access: String
    "The refresh code that should be used to generate a new access code after the current session has ended"
    refresh: String
}

type RootQueryType {
    "Authenticate with a email and password"
    authenticate(
        "Email to authenticate with"
        email: String!,
        "Password to authenticate with"
        password: String!
    ): AuthDetails
    ping: String
}
