import {Meta, Canvas, Story, ArgsTable} from "@storybook/addon-docs/blocks";
import Login from "./Login";

<Meta title="Application/Menus/Login" component={Login}/>

# Login Menu
The only purpose of this component is to be rendered into the LoginPage root component.

## Usage
Pretty much only 1 intended way to use it.

When turning the float property off, the formatting unreliably breaks randomly.

<Canvas>
    <Story name="Intended" args={{float: false}}>{args => <Login {...args}/>}</Story>
</Canvas>

## API

<ArgsTable of={Login}/>
