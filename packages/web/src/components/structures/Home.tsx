import React from "react";

import { ApplyGlobalStyles } from "../helpers/styles";

export const Home: React.FC = (_p) => {
    return (
        <ApplyGlobalStyles>
            <p>Implementing in next commits</p>
        </ApplyGlobalStyles>
    );
};
