import ${NAME} from "./${NAME}";
import {ArgsTable, Canvas, Meta, Story} from "@storybook/addon-docs/blocks";

<Meta title="${NAME}" component={${NAME}}/>

# ${NAME}
This is a component.

#[[## Usage]]#
#if($USAGE != "") $USAGE #else You use it like this #end

<Canvas>
    <Story
        name="default"
        args={{}}
    >args => <${NAME} {...args}/>}</Story>
</Canvas>

#[[## API]]#

<ArgsTable of={${NAME}}/>
