{
  "name": "collaborated",
  "workspaces": [
    "core/*",
    "packages/*",
    "scripts",
    "test/*"
  ],
  "private": true,
  "license": "GPL-3.0",
  "scripts": {
    "terminals": "yarn workspace scripts terminals",
    "docs": "yarn workspace web docs",
    "docsync": "git fetch && git pull && git push",
    "buildts": "yarn workspace server tsc && yarn workspace @collaborated/interact tsc && yarn workspace test-addon tsc",
    "buildall": "yarn buildts && yarn docs && yarn workspace web snowpack build"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
