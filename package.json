{
  "name": "collaborated",
  "workspaces": [
    "packages/*",
    "scripts",
    "test/*"
  ],
  "private": true,
  "license": "GPL-3.0",
  "scripts": {
    "docs": "yarn workspace web docs",
    "docsync": "git fetch && git pull && git push",
    "buildts": "yarn workspace @collaborated/common tsc && yarn workspace @collaborated/interact tsc && yarn workspace test-addon tsc && yarn workspace server tsc && yarn workspace app tsc",
    "buildall": "yarn buildts && yarn docs && yarn workspace web snowpack build",
    "unpushed": "git log origin/master..HEAD",
    "netlifydeploy": "yarn && yarn workspace @collaborated/interact tsc && yarn workspace web snowpack build",
    "docsend": "git add docs && git subtree push --prefix docs origin gh-pages && git push",
    "mbuildlinux": "cd services/manager && go build -v -x -o ../../../capp main.go && cd ../../",
    "mbuildwin": "cd services/manager && go build -v -x -o ../../../capp.exe main.go && cd ../../",
    "manager": "yarn mbuildwin && cd ../ && \"./capp.exe\"",
    "firstmbuild": "go install -v -x -a -i && yarn manager",
    "lint": "yarn eslint . --ext .js,.jsx,.ts,.tsx",
    "build": "yarn && yarn workspace scripts tsc && yarn workspace scripts node dist/build.js -v",
    "tsbuild": "yarn build -t",
    "devbuild": "yarn build --dev",
    "prepack": "yarn build"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^4.12.0",
    "@typescript-eslint/parser": "^4.12.0",
    "all-contributors-cli": "^6.19.0",
    "eslint": "^7.17.0",
    "eslint-config-prettier": "^7.1.0",
    "eslint-plugin-react": "^7.22.0",
    "eslint-plugin-workspaces": "^0.6.2",
    "lerna": "^3.22.1"
  },
  "version": "0.0.0",
  "buildOrder": [
    "common",
    "interact",
    "web",
    "app",
    "server"
  ]
}
